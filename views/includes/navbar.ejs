<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top shadow-sm py-2.5">
    <div class="container-fluid px-4">
        
        <!-- Back Button & Brand Container -->
        <div class="d-flex align-items-center">
            <% 
                const isHomePage = (
                    typeof currentPath !== 'undefined' &&
                    (currentPath === '/' || currentPath === '/products')
                );

                if (currentUser && !isHomePage) { 
            %>
                <button 
                    type="button" 
                    class="btn btn-outline-secondary btn-sm me-3 d-inline-flex align-items-center rounded-pill px-3 text-light border-secondary" 
                    onclick="history.back()" 
                    title="Go Back"
                    style="font-size: 0.8rem;"
                >
                    <i class="bi bi-arrow-left me-1"></i> Back
                </button>
            <% } %>

            <!-- Brand -->
            <a class="navbar-brand fw-bold fs-4 text-primary d-flex align-items-center gap-1" href="/products">
                ShopHub
            </a>
        </div>

        <!-- Mobile Toggler -->
        <button
            class="navbar-toggler border-0"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbar"
            aria-controls="navbar"
            aria-expanded="false"
            aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navigation Links -->
        <div class="collapse navbar-collapse" id="navbar">

            <!-- Left Side Links -->
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 align-items-lg-center">
                <li class="nav-item">
                    <a class="nav-link fw-medium" href="/products">
                        Home
                    </a>
                </li>

                <% if (currentUser) { %>
                    <li class="nav-item">
                        <a class="nav-link text-success fw-semibold d-inline-flex align-items-center gap-1" href="/products/new">
                            <i class="bi bi-plus-lg"></i> Add Product
                        </a>
                    </li>
                <% } %>
            </ul>

            <!-- Right Side Links -->
            <ul class="navbar-nav ms-auto align-items-lg-center gap-lg-1">

                <% if (currentUser) { %>
                    <!-- Wishlist -->
                    <li class="nav-item">
                        <a class="nav-link d-flex align-items-center gap-1" href="/wishlist">
                            <i class="bi bi-heart text-danger"></i> Wishlist
                        </a>
                    </li>
                <% } %>

                <!-- Cart -->
                <li class="nav-item">
                    <a class="nav-link position-relative d-flex align-items-center gap-1 me-lg-2" href="/cart">
                        <i class="bi bi-cart3"></i> Cart
                        <% if (typeof cartCount !== 'undefined' && cartCount > 0) { %>
                            <span class="badge bg-danger rounded-pill px-2 py-1" style="font-size: 0.65rem;">
                                <%= cartCount %>
                            </span>
                        <% } %>
                    </a>
                </li>

                <% if (currentUser) { %>
                    <!-- My Products -->
                    <li class="nav-item">
                        <a class="nav-link d-flex align-items-center gap-1" href="/products/my-products">
                            <i class="bi bi-box-seam"></i> My Products
                        </a>
                    </li>

                    <!-- Recently Viewed -->
                    <li class="nav-item">
                        <a class="nav-link d-flex align-items-center gap-1" href="/products/recently-viewed">
                            <i class="bi bi-clock-history"></i> Recently Viewed
                        </a>
                    </li>

                    <!-- My Orders -->
                    <li class="nav-item">
                        <a class="nav-link me-lg-2" href="/orders">
                            My Orders
                        </a>
                    </li>

                    <!-- Logout Button -->
                    <li class="nav-item mt-2 mt-lg-0">
                        <a class="btn btn-outline-danger btn-sm px-3 rounded-pill fw-semibold" href="/logout">
                            Logout
                        </a>
                    </li>

                <% } else { %>

                    <!-- Sign Up -->
                    <li class="nav-item mt-2 mt-lg-0">
                        <a class="btn btn-outline-light btn-sm px-3 rounded-pill me-lg-2 fw-medium" href="/signup">
                            Sign Up
                        </a>
                    </li>

                    <!-- Login -->
                    <li class="nav-item mt-2 mt-lg-0">
                        <a class="btn btn-primary btn-sm px-3 rounded-pill fw-semibold" href="/login">
                            Log In
                        </a>
                    </li>

                <% } %>

            </ul>

        </div>

    </div>
</nav>